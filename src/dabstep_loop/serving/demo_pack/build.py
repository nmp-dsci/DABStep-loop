"""Build the demo replay pack. Written in M5."""

from __future__ import annotations

from pathlib import Path


def build_pack(run_id: str | None = None) -> Path:
    raise NotImplementedError("M5")
